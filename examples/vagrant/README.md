# Install Hashicorp's vagrant
Install vagrant on Ubuntu

# To try it out

To try this out:

1. Spin up a test VM

    vagrant up

2. From this directory, run:

    pyinfra --user vagrant --password vagrant @vagrant vagrant.py

or

    pyinfra @vagrant vagrant.py
