## pyinfra.modules.pkg

Manage BSD packages and repositories. Note that BSD package names are case-sensitive.

Uses:

+ `pkg_add`
+ `pkg_delete`

##### function: packages

```py
packages(
    packages=None,
    present=True
)
```
