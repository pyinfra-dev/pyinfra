{
    "args": ["filename", "match", "replace"],
    "facts": {
        "files.FindInFile": {
            "extended_regex=False, interpolate_variables=False, path=filename, pattern=match": []
        }
    },
    "commands": [],
    "noop_description": "string \"match\" does not exist in filename"
}
