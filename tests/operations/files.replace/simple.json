{
    "args": ["filename", "match", "replace"],
    "kwargs": {
        "flags": ["g", "i"]
    },
    "commands": [
        "sed \"s/match/replace/gi\" filename > _tempfile_",
        "mv _tempfile_ filename"
    ]
}
