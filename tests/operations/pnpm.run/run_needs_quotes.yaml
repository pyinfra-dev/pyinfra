args:
  - "build; rm -rf /"
  - "/app dir; rm -rf /"
kwargs:
  args: ["--out", "/tmp/dist dir; reboot"]
commands:
  - "pnpm --dir '/app dir; rm -rf /' run 'build; rm -rf /' --out '/tmp/dist dir; reboot'"
