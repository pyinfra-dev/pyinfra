args:
  - ["github.com/foo/bar/v2"]
kwargs:
  present: false
facts:
  go.GoPackages:
    github.com/foo/bar/v2: ["v2.0.5"]
commands:
  - 'BINDIR=$(go env GOBIN); [ -z "$BINDIR" ] && BINDIR="$(go env GOPATH)/bin"; rm -f "$BINDIR"/bar'
