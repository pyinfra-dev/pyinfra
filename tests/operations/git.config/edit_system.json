{
    "args": ["key", "value"],
    "kwargs": {
        "system": true
    },
    "facts": {
        "git.GitConfig": {
            "key": ["wrongvalue"]
        }
    },
    "commands": [
        "git config --system key \"value\""
    ]
}
