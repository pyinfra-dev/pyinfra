{
    "args": ["key", "value"],
    "kwargs": {
        "repo": "/my/repo"
    },
    "facts": {
        "git_config": {
            "/my/repo": {
                "another_key": "value"
            }
        }
    },
    "commands": [
        "cd /my/repo && git config --local key \"value\""
    ]
}
