{
    "args": ["logrotate.timer"],
    "facts": {
        "systemd.SystemdStatus": {
            "logrotate.timer": false
        }
    },
    "commands": [
        "systemctl start logrotate.timer"
    ]
}
