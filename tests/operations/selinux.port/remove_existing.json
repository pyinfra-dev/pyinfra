{
    "args": ["tcp", 22, "ssh_port_t"],
    "kwargs": {
        "present":false
    },
    "facts": {
        "selinux.SEPort": {
            "tcp": {"22":  "ssh_port_t"}
        }
    },
    "commands": [
        "semanage port -d -p tcp 22"
    ]
}
