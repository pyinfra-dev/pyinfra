{
    "args": ["/srv/app"],
    "kwargs": {
        "build": true,
        "force_recreate": true,
        "remove_orphans": true
    },
    "commands": [
        "docker compose --project-directory /srv/app up -d --build --force-recreate --remove-orphans"
    ]
}
