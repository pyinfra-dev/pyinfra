{
    "args": ["somefile"],
    "kwargs": {
        "database": "somedb",
        "psql_user": "root",
        "psql_password": "somepass"
    },
    "commands": [
        [
            "PGPASSWORD=\"somepass\" psql -d somedb -U root < somefile",
            "*** psql -d somedb -U root < somefile"
        ]
    ],
    "idempotent": false
}
