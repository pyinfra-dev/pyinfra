{
    "args": ["somefile", "match line"],
    "facts": {
        "files.FindInFile": {
            "path=somefile, pattern=^.*match line.*$": []
        }
    },
    "commands": [
        "echo 'match line' >> somefile"
    ]
}
