{
    "args": ["somefile", "match line"],
    "facts": {
        "files.FindInFile": {
            "extended_regex=False, interpolate_variables=False, path=somefile, pattern=^.*match line.*$": []
        }
    },
    "commands": [
        "echo 'match line' >> somefile"
    ]
}
