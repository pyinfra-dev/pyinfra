{
    "args": ["somefile", "match_line"],
    "kwargs": {
        "replace": "replace_line",
        "flags": "abc"
    },
    "facts": {
        "files.FindInFile": {
            "extended_regex=False, interpolate_variables=False, path=somefile, pattern=^.*match_line.*$": [],
            "extended_regex=False, interpolate_variables=False, path=somefile, pattern=^replace_line$": []
        }
    },
    "commands": [
        "echo replace_line >> somefile"
    ]
}
