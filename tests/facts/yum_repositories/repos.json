{
    "command": "(! stat /etc/yum.conf 2> /dev/null || cat /etc/yum.conf) && (! stat /etc/yum.repos.d/*.repo 2> /dev/null || cat /etc/yum.repos.d/*.repo)",
    "output": [
        "# comment line",
        " # comment line with whitespaces",
        "[somerepo]",
        "baseurl=abc"

    ],
    "fact": [
        {
            "name": "somerepo",
            "baseurl": "abc"
        }
    ]
}
