command: "{ s6-rc -a list && echo -e 'GROUP SEPARATOR' && s6-rc -da list ; } || exit 1"
requires_command: "s6-rc"
output: |
  GROUP SEPARATOR
  cupsd
  usbguard
  avahi-daemon-log
  avahi-daemon-srv
fact:
  cupsd: false
  usbguard: false
  avahi-daemon-log: false
  avahi-daemon-srv: false

