arg:
  directory: "/app dir; rm -rf /"
command: "! test -d '/app dir; rm -rf /' || pnpm --dir '/app dir; rm -rf /' list --depth=0 --json"
requires_command: pnpm
output: |
  [
    {
      "name": "app",
      "version": "1.0.0",
      "path": "/app dir; rm -rf /",
      "private": false
    }
  ]
fact: {}
