{
    "command": "(! stat /etc/dnf.conf 2> /dev/null || cat /etc/dnf.conf) && (! stat /etc/dnf.repos.d/*.repo 2> /dev/null || cat /etc/dnf.repos.d/*.repo) && (! stat /etc/yum.repos.d/*.repo 2> /dev/null || cat /etc/yum.repos.d/*.repo)",
    "output": [
        "",
        "[somerepo]",
        "baseurl=abc",
        "",
        "[anotherrepo]"
    ],
    "fact": [
        {
            "name": "somerepo",
            "baseurl": "abc"
        },
        {
            "name": "anotherrepo"
        }
    ]
}
