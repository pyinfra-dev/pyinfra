arg:
  - "/etc/s6/repo"
command: "s6-rc-repo-list -r /etc/s6/repo"
requires_command: "s6-rc-repo-list"
output: |
  default
  minimal
  recovery
  kiosk
fact:
  [ "default", "minimal", "recovery", "kiosk" ]
