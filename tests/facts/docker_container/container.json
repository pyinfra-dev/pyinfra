{
    "arg": "myid",
    "command": "which docker > /dev/null && docker container inspect myid || true",
    "output": [
        "{\"hello\": \"world\"}"
    ],
    "fact": {
        "hello": "world"
    }
}
