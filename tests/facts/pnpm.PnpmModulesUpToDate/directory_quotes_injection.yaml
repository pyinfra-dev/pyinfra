arg:
  directory: "/app dir; rm -rf /"
command: "if cmp -s '/app dir; rm -rf /pnpm-lock.yaml' '/app dir; rm -rf /node_modules/.pnpm/lock.yaml' ; then echo up_to_date; else echo not_up_to_date; fi"
output: |
  not_up_to_date
fact: false
