{
    "command": "container_ids=$(docker ps -qa) && [ -n \"$container_ids\" ] && docker container inspect $container_ids || echo \"[]\"",
    "requires_command": "docker",
    "output": [
        "{\"hello\": \"world\"}"
    ],
    "fact": {
        "hello": "world"
    }
}
